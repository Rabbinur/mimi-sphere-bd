import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact Shopping Cart BD | Import & Global Sourcing Experts",
  description: "Get in touch with our trade specialists for Export-Import, LC handling, and global sourcing inquiries. We're here to help your business grow.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
